/*const estatus = "pendiente";
const pagado = true;

if (estatus === "Completado" && pagado === true){
  console.log("el pedido esta completado y pagado");
}/* else if ( estatus === "pendiente" && pagado === true){
  console.log ("el pedido esta en camino");
} else if (estatus === null || pagado === null){
  console.log("el pedido no se ha creado");
}else if (estatus !== null && pagado !== null){
  console.log("el pedido esta pendiente de pago");
}*/

/*const nombre = null;
const nombreDefecto = "invitado";

const nombreUsuario = nombre ?? nombreDefecto;

console.log(nombreUsuario);*/

const hora = 14
const saludo = hora < 12 ? "buenos días" : "Buenas tardes";
console.log(saludo);



/* AND &&, OR ||, NOT !, NULLISH COALASCING ?? */
/* TERNARIO (CONDICION ? TRUE : FALSE)*/
